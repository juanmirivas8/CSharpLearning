using OOP.Models;

// A parameterless constructor is needed to use this syntax, otherwise you would have to fill arguments first
var Juan = new PersonClass("Juan",2) //Because there is a parameterless constructor this is redundant
{
    Age = 2,
    Name = "Juan"
};